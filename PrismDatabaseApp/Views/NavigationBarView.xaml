<UserControl x:Class="PrismDatabaseApp.Views.NavigationBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity">
    <StackPanel>
        
        <TextBlock Text="Smart Factory" Foreground="White" FontSize="24" Margin="10"  FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Button Content="Slurry Supply Process" FontWeight="Black"
                Command="{Binding NavigateCommand}" 
                CommandParameter="SlurrySupplyProcessView" 
                 />
        <Button Content="Coating Process" FontWeight="Black"
                Command="{Binding NavigateCommand}" 
                CommandParameter="CoatingProcessView" 
                 />
        <Button Content="Drying Process" FontWeight="Black"
                Command="{Binding NavigateCommand}" 
                CommandParameter="DryingProcessView" 
                 />
        <Button Content="Notification Inquiry" FontWeight="Black"
                Command="{Binding NavigateCommand}" 
                CommandParameter="NotificationInquiryView" 
                 />
    </StackPanel>
</UserControl>
